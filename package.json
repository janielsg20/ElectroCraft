{
  "name": "electrocraft",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "studio:dev": "pnpm --filter studio dev"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^1.0.0",
    "pnpm": "^8.0.0"
  }
}
